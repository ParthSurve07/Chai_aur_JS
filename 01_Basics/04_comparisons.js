// console.log("2" > 1)
// console.log("02" > 1)

// Equality operator (==) does not consider the type of the operands
// but when we use >= or <=, it converts the operands to numbers

console.log(null > 0)
console.log(null == 0)
console.log(null >= 0)

// === is the strict equality operator
// It checks the type of the operands as well as the value